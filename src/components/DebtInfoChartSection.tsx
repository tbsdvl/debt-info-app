import Section from './Section.tsx';
import DebtInfoChart from './DebtInfoChart.tsx';

const DebtInfoChartSection = () => {
  return <Section id={'chart'} component={<DebtInfoChart />} />;
}

export default DebtInfoChartSection;